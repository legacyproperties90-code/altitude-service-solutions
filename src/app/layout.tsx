import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Altitude Service Solutions | Professional Pressure Washing",
  description: "Professional pressure washing, soft washing, roof cleaning, and exterior cleaning services in Atlanta, GA. Residential & Commercial. Free quotes available.",
  keywords: "pressure washing Atlanta, soft washing, roof cleaning, house washing, commercial pressure washing, Lawrenceville GA",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} bg-[#0A1628] text-white min-h-screen flex flex-col`}>
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
